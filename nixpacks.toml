[phases.setup]
nixPkgs = ["nodejs_22"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma db push --accept-data-loss", "npm run build"]

[start]
cmd = "npx next start -p $PORT"
