[phases.setup]
nixPkgs = ["nodejs_22"]

[variables]
NODE_ENV = "production"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma db push --accept-data-loss", "npm run build"]

[start]
cmd = "npx next start -p 3000"
