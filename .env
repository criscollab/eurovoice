DATABASE_URL=file:/home/z/my-project/db/custom.db

# NextAuth secret — used to sign JWTs. Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=eurovoice-secret-key-change-in-production-2024-xyz

# NextAuth URL — the canonical URL of your site
NEXTAUTH_URL=http://localhost:3000

# Admin credentials — the single user who can manage stations & songs
ADMIN_USERNAME=admin
ADMIN_PASSWORD=eurovoice2024
